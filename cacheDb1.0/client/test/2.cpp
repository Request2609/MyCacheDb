#include <iostream>
#include <vector>
#include <string.h>
using namespace std ;

vector<string> split(string dd, const string ss) {
    
    const char* p = ss.c_str() ;
    char* p = strtok(dd.c_str(), p) ;

}

int main() {
    
}
